
def main():
    s = "wednesday"
    #print(s.capitalize()) # temporarily capitalizes
    #print(s.isalpha()) # checks if s is only letters (alphabet)
    #print(s.isdigit()) # checks if s is only digits
    #print(s.partition("d")) # partitions on letter d
    #print(s.split("d")) # ignores the letter d
    #print(s.startswith("w"))
    #print(s.endswith("ay"))
    print(list(s)) # each element of string split into a list

main()
